__pycache__/
*.py[cod]
.venv/
.env
*.db
.pytest_cache/
.mypy_cache/
mig_test.db
*.pdf

# Local-machine TLS-interception CA (Geder filter), needed only to build
# through a network proxy on this dev machine. Never commit; build-only,
# trusted in an intermediate Docker stage and never copied into the final
# image (see Dockerfile).
geder-filter-ca.crt.local

# Leftover local run/log artifacts from a third-party dev-preview tool.
.freebuff/

# Machine-local agent notes (env quirks, not for the public repo)
CLAUDE.local.md
